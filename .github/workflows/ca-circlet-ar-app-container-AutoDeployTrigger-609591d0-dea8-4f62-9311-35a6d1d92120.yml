name: Trigger auto deployment for ca-circlet-ar-app-container

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ca-circlet-ar-app-container-AutoDeployTrigger-609591d0-dea8-4f62-9311-35a6d1d92120.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CACIRCLETARAPPCONTAINER_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CACIRCLETARAPPCONTAINER_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CACIRCLETARAPPCONTAINER_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.CACIRCLETARAPPCONTAINER_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CACIRCLETARAPPCONTAINER_REGISTRY_PASSWORD }}
          containerAppName: ca-circlet-ar-app-container
          resourceGroup: rg-circletdemo03-CT-AR-app
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


